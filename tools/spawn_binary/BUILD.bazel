load("@io_bazel_rules_go//go:def.bzl", "go_binary", "go_library")

go_library(
    name = "spawn_binary_lib",
    srcs = [
        "main.go",
        "signals_unix.go",
        "signals_windows.go",
    ],
    importpath = "github.com/bazel-contrib/bazel-lib/tools/spawn_binary",
    visibility = ["//visibility:public"],
)

go_binary(
    name = "spawn_binary",
    embed = [":spawn_binary_lib"],
    visibility = ["//visibility:public"],
)
